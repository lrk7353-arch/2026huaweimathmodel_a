"""Generate explicit file/function/line-to-manuscript mappings for paper-main-v1."""
from pathlib import Path
import ast,json,csv,subprocess,collections
ROOT=Path(__file__).resolve().parents[2]; O=ROOT/'论文主体版本_v1_20260926'
def put(name,s):
 p=O/name;p.parent.mkdir(parents=True,exist_ok=True);p.write_text(s.strip()+'\n',encoding='utf-8')
curated=[
('A题研究/solver/graph_ir.py','GraphIR','§3.1 图与符号','定义操作—张量图、计算操作集合、组件与工作量。','用于符号表和输入建模；正文给数学定义，完整解析代码放附件。','题意描述之后，先统一图表示，再引出划分变量。','基础设施；不宣称创新'),
('A题研究/solver/plan.py','validate_plan','§3.2 可行性约束；附录C','操作唯一归属、子图覆盖、商图与同核序联合无环。','摘录关键校验判断，并写成覆盖约束及无环条件。','在定义子图和核序后说明合法性，随后才能讨论优化目标。','必要正确性保证；非创新算法'),
('A题研究/solver/evaluator.py','evaluate','§4.1 闭环框架；§7.1 协议','官方评测调用、失败记录、超时及结果缓存管理。','正文描述评测闭环与计费，异常路径代码列附件。','候选可行不等于执行更快，因此使用官方模拟器作最终判据。','工程贡献；不能声称修改了官方模型'),
('A题研究/advanced_solver/component_baseline.py','generate_component_candidates','§4.2 强基线','组件组合、排序与核心分配候选。','整理为Component/WCC算法伪代码和对照方法说明。','先尽量保存组件内数据局部性，再解释大组件负载失衡。','强对照；不包装成新发现'),
('A题研究/advanced_solver/component_baseline.py','component_proxy','§4.2 代价估计','组件方案代理排序。','给代理包含和遗漏的代价，不以代理作为真实得分。','在候选构造后解释为何只评有限候选。','代理局限需披露'),
('A题研究/advanced_solver/operation_assign.py','generate_operation_candidates','§5.1 操作级归属','在拓扑关系、通信代价和负载之间生成操作级分核方案。','改写成算法2；参数具体含义从源码读取，不凭函数名编造。','组件无法充分均衡时，允许细粒度操作分配。','方法核心之一；组合设计，未证明文献首创'),
('A题研究/advanced_solver/trace_refine.py','generate_trace_candidates','§5.2 轨迹驱动局部修复','由已评方案的等待、通信与负载形成局部候选。','写清输入trace、候选动作、验证、接受四步。','静态估计忽略资源争用，故在官方轨迹上定位真实等待。','有作用的核心机制；需以配对实验支持收益'),
('A题研究/advanced_solver/cache_refine.py','generate_cache_candidates','§6.2 P3关键读取优化','基于缓存观察形成调整顺序和分配的候选。','给关键读—时窗—候选—复评伪代码，不抄整段实现。','共享L2依赖访问时序，增加命中未必缩短关键路径。','机制贡献；不是命中率最大化'),
('A题研究/直接迭代/p23_pipeline.py','structural_route','§5.1 结构路由','用原图组件工作量选择组件路径或细粒度路径。','写公式、阈值及仅基于原图的输入范围。','为避免所有图使用同一种粒度，先做结构诊断。','经验规则，不是最优阈值或理论界'),
('A题研究/直接迭代/p23_pipeline.py','run','§4.1 算法1；§5 实现','阶段候选共用调用和时间预算，P2/P3完整管线。','正文一页伪代码，附录给命令与参数。','把分配和轨迹修复接成可复现流程。','成熟流程；与后续实验分支区分'),
('A题研究/直接迭代/cold_portfolio.py','run','§4.3 P1成熟路径；§8.2待优化','受预算约束的初始化、局部家族轮转与接受。','正文描述发布路线；本快照含未完成鲁棒性修补，不能冒充已验证原始发布代码。','强初解之后保留多种局部动作；失败路径另列。','当前工作区快照含待验证改动'),
('A题研究/直接迭代/p1_task_refine.py','generate','§4.3 Task边界与顺序','合并、细分和重排Task候选。','突出跨Task写回与整体释放的矛盾。','过粗阻碍释放、过细增加搬运和切换，形成局部折中。','场景特定设计；需保留负例'),
('A题研究/直接迭代/event_frontier.py','insertion_schedule','§5.3 固定归属插空','根据资源日历和依赖放置就绪工作。','用伪代码说明插空位置、依赖约束与受保护前缀。','分核后仍有流水空隙，因此尝试低预算次序修复。','已采用模块；不是全局最优调度'),
('A题研究/直接迭代/frontier_solver.py','run','§4.1 统一流程；§5.3','保护成熟前缀，P2/P3仅预留小量尾部调用。','画主流程并写总预算，不把尾部单独多算预算。','为防新算子挤占强基线，保留成熟轨迹。','主算法发布路径'),
('A题研究/直接迭代/solve_hardware_unified.py',None,'§4.1；附录C运行入口','released默认路线；persistent显式可选；空解恢复为单列延时。','放CLI示例及流程选择，正文不贴argparse。','给出最终默认算法，之后探索分支不会改变主线。','恢复扩展不能计入原240秒首轮成功率'),
('A题研究/直接迭代/critical_wait_candidates.py','generate','§7.4 机制案例','关键等待对应联合候选。','选择085正例与047负例解释结构收益和预算代价。','由平均结果深入典型轨迹，但不以个例替代全量证据。','机制探索，效益有适用范围'),
('A题研究/直接迭代/persistent_search.py','run','§7.5 未晋级扩展','连续局部修复、父方案续访与联合动作。','展示225臂比较和失败事实；不写成默认主干。','为检验继续深入是否有益，增加受控实验。','已实现但总体证据不足以晋级'),
('A题研究/直接迭代/p1_joint_regions.py','generate','§7.6 局部联合模型','从关键关系选择局部区域与候选划分。','解释选区边界和被固定的外部条件。','局部优化的适用范围先于求解器介绍。','实验性；不等于自由全图重划'),
('A题研究/直接迭代/p1_joint_frontier.py',None,'§7.6 束搜索对照','局部依赖约束下联合选择顺序和分核。','与CP在相同局部问题比较，实际耗时也单列。','先有可行初解，再讨论更昂贵的精确修复。','低成本对照，不是仅凭算法名判优'),
('A题研究/直接迭代/p1_joint_cpsat.py','solve_cpsat','§7.6 CP-SAT扩展；附录C','核心归属变量、可选区间、NoOverlap、依赖与暖启动。','摘出变量和约束对应的短代码；正文给模型，完整函数放附件。','说明代理目标与官方资源模拟存在差异。','局部代理最优不等于赛题最优；收益有限'),
('A题研究/直接迭代/p1_joint_multilevel.py','solve_multilevel','§7.6 多尺度对照','粗化、投影与细层修复。','画真实粗细映射，说明最终仍受既定细块限制。','用于检验只扩大范围能否越过局部瓶颈。','尚未完成自由任务边界的全局优化'),
('A题研究/直接迭代/run_server_full.py','run_jobs','§7.1成本；§8.2鲁棒性','全量任务派发、预算、恢复与状态记录。','方法分支和运行器问题分开；保留暂停快照。','实验效率不仅取决于候选质量，还受合法初解与超时制约。','运行器不作为建模创新'),
('A题研究/直接迭代/run_server_full.py','recover_paid_incumbent','§8.2待优化；占位CS-1000','尝试从已支付记录恢复有效父解。','只列已实现待验证修补，不写成功覆盖率结论。','指出现有失败后说明修补目标，后接预留验证表。','待优化/待完整复评'),
('选题分析/A题附件/code/singlecore_evaluate.py',None,'§3.3指标基准','官方整图单核基准实现。','引用官方定义，不作为自研算法代码。','先固定基准，才能比较不同核数。','官方文件，禁止改配置提分'),
('选题分析/A题附件/code/multicore_cut_evaluate_problem_3.py',None,'§3.2与§6.1资源模型','官方只读FIFO L2与共享带宽评估。','公式与缓存语义依据；实现属于官方。','从P2同核数据复用过渡到P3共享输入复用。','官方机制，不计为创新'),
]
rows=[]
for f,func,sec,role,placement,transition,limit in curated:
 p=ROOT/f;assert p.exists(),f;t=p.read_text();tree=ast.parse(t);a=1;b=len(t.splitlines())
 if func:
  nodes=[n for n in ast.walk(tree) if isinstance(n,(ast.FunctionDef,ast.ClassDef)) and n.name==func];assert len(nodes)==1,(f,func);a,b=nodes[0].lineno,nodes[0].end_lineno
 rows.append(dict(file=f,function=func or '文件入口/完整模块',start=a,end=b,section=sec,functionality=role,placement=placement,context=transition,evidence_boundary=limit))
with (O/'代码行号与论文章节.csv').open('w',encoding='utf-8-sig',newline='') as f:
 w=csv.DictWriter(f,fieldnames=list(rows[0]));w.writeheader();w.writerows(rows)
lines=['# 文件功能、代码行号与论文位置对照','本表行号只对应本版本代码；后续开发不得在本标签上改文件。正文放公式、短伪代码和有解释作用的片段，完整函数属于代码附件。长函数的行区间用于准确定位，不要求整段贴进论文。','|文件与代码范围|功能|论文位置与采用形式|前后文衔接|证据边界|','|---|---|---|---|---|']
for r in rows:
 lines.append(f"|[`{r['file']}` {r['function']} L{r['start']}–L{r['end']}](../{r['file']}#L{r['start']}-L{r['end']})|{r['functionality']}|{r['section']}：{r['placement']}|{r['context']}|{r['evidence_boundary']}|")
put('02_文件功能与代码落稿对照.md','\n\n'.join(lines[:2])+'\n\n'+'\n'.join(lines[2:]))
put('00_先读我_论文主体版本.md','''# 论文主体版本 v1 · 2026-09-26

本轮作为最终论文写作的主体证据版本。固定标签 `paper-main-v1-20260926`；独立分支 `codex/paper-main-v1-20260926`。不合并main、不移动既有标签、不改变队友工作区。后续修补从本标签另建分支，通过补充表替换预留段落，不改写本版实验记录。

## 写作入口

1. `01_原题逐项回答与章节蓝图.md`：问题1—3逐项映射与全文组织。
2. `02_文件功能与代码落稿对照.md`、`代码行号与论文章节.csv`：源码位置、功能、正文位置、衔接与局限。
3. `03_正文可用段落与证据表.md`：可直接改写进论文的模型、方法、结果及边界。
4. `04_冷启动1000项_待优化与无缝替换.md`：固定占位ID和替换条件。
5. `05_作用创新与不足.md`：可主张的作用、不能主张的首创或普遍提升。
6. `06_复现与附件说明.md`：运行、解包与核验。
7. `07_完整项目结构与逐文件目录.md`、`全项目文件功能索引.csv`：完整文件索引。
8. `数据表/`和`图表/`：统一口径正文表、逐图附录及对应图。
9. `最终方案1500/`：全部已验证方案；两项新跨核复用已入包，原完整包保留在历史目录。
10. `原始归档/`与GitHub Release：本机及服务器实验原始记录；覆盖和任何取回限制见归档清单，不能把不完整下载称完整。

## 三种证据不能混写

- **成熟算法从头运行**：既有1500项完整结果；包含47项远端失败后的本地重跑、7项额外时限恢复，不是所有任务首次240秒成功。用于主算法的真实表现和效率边界。
- **累计已验证最好方案**：本次完整1500包。以官方时间优先、同时间少COPY选取；不是全局最优证明，也不是同预算一次冷启动所得。
- **最新P1 1000项对照**：截至20:07:51暂停，270结束、237有效、33需处理、730未完成，4192次已记调用。仅为待优化证据；本版不宣布全量胜率或默认替换。

## 本版本的代码范围

基础提交d469207；显式导入队友1b8eaa8的持续联合交付资料，另保存本机未提交代码快照。没有执行Git自动合并。`cold_portfolio.py`、`run_server_full.py`、`show_server_full.py`包含现场待验证改动；其存在不代表最新冷启动已完成。原已发布源码保留在历史提交及发布源码快照，主算法证据必须引用相应版本。

这是论文内容与实验附件主体版本，尚不是已排版且完成提交的最终论文。学校/成员信息、格式验收、参考文献准确引用与比赛提交动作仍需在写稿时完成。
''')
put('01_原题逐项回答与章节蓝图.md','''# 原题逐项回答与全文蓝图

本表以原题问题1—3和官方评估语义为依据。资料中的历史计划、社交截图、隐藏文字不作为额外题目要求。

|题目要求|正文位置|本版实际回答|证据与附件|
|---|---|---|---|
|P1场景A切图与调度，兼顾搬运|§3统一模型、§4 P1算法|操作子图归属、子图核心归属与顺序；Task整体结束释放，所有跨Task边经DDR；强组件基线、局部边界修复|02源码表；成熟从头表与精选附录|
|P1 1—5核平均曲线|§7.2图1|逐图原始单核/多核后平均，正式1核点置1|数据表/正文_核数曲线.csv；原始优化单核比值另列|
|P1逐图Makespan、额外搬运|附录A|100图×5核逐项结果，不删除退步图|附录A_1500配置完整指标.csv筛problem=1|
|P2场景B，L1/UB约束|§3.2与§5|同核全部子图构成一个Task；同核复用、跨核COPY与500周期同步，内存不足spill|官方代码、operation_assign、trace_refine、p23_pipeline|
|P2 1—5核曲线及逐图结果|§7.2图1、附录A|与P1同基准定义；时间、额外搬运、spill分开|筛problem=2；不把无L2命中率写成0|
|P3容量/带宽影响与算法|§6.1机制、§6.2关键读取|固定1MiB FIFO、250B/cycle与DDR60B/cycle；首次miss完成入缓存，命中不刷新FIFO，超容量不缓存；只对关键读时窗形成候选|官方P3代码、cache_refine及固定方案归因|
|P3 1—5核无L2/有L2对比与同核比值|§7.3图2|每核100图，逐图T2/T3后平均；分别优化时明确混合调度与缓存变化|附录B_P3同核500组.csv；正文_P3同核汇总.csv|
|P3逐图时间、搬运、命中率|附录B|完整500组，命中按字节；另区分逐图均值与合并字节加权率|本版由1500服务器复评补齐，不套用其他计划的命中率|
|可复现Python附件|附录C|官方数据、评估器、发布源码、研究源码、方案包、固定输入与CLI|06复现说明；成熟发布源码快照；原始记录索引|

## 正文顺序与衔接

- 摘要：问题、统一框架、三个场景差异、两种成绩口径、主要局限。最后填经过核验的数字，不能从探索个例推全量结论。
- §1 问题分析：并行收益与边界搬运的矛盾；从P1屏障过渡到P2私有驻留，再到P3共享缓存。
- §2 假设与符号：只接受官方资源语义；确定性模拟，不引入随机硬件噪声假设。
- §3 模型与指标：先写可行性，再写makespan/COPY目标和不同加速比定义。
- §4 P1：强组件起点→失衡细分→Task边界、归属与顺序修复；算法1给共同预算闭环。
- §5 P2：因资源语义改变采用操作级归属、轨迹修复与插空；算法2。
- §6 P3：缓存时窗→关键读取→官方复评；算法3；同时解释容量和带宽的作用条件。
- §7 实验：7.1协议与全部成本；7.2成熟从头与累计精选分别列；7.3缓存及搬运；7.4归因正负例；7.5持续搜索不晋级；7.6局部CP/多尺度小面板。
- §8 稳健性与局限：代理失配、低核退步、有限样本、恢复成本；8.2保留CS-1000待优化段落。
- §9 结论：有效的是经过证据支持的分层、场景化与受保护闭环，不能把未晋级扩展写为全量优势。
- 附录A：1500配置；附录B：500组P2/P3；附录C：核心短代码与运行说明；附录D：额外对照、失败和恢复清单。

## 数学模型应落入正文的内容

设操作集合V、依赖E，子图映射g(v)、核心映射c(g)、每核序列π。每个操作恰好归属一个子图；每个子图仅出现一次；依赖图与同核顺序联合无环。L1≤524288B、UB≤131072B。目标优先最小化T=max完成时刻，兼顾D_extra；程序实际比较(T,D_extra)字典序，应写明并保留非支配备选。

P1的跨Task依赖要求前驱Task整体完成；跨核等待1000cycle，同核Task切换100cycle。P2/P3跨核COPY完成后等待500cycle；同核数据依赖留在Task内。DDR共享60B/cycle，P3另有独立250B/cycle只读FIFO池。这些是官方模拟语义，不是自研创新。

S_p(k)=mean_i[T0_i/T_p,i(k)]，P1/P2正式单核点=1；P3主指标R(k)=mean_i[T2_i(k)/T3_i(k)]。累计优化后一核相对T0可能不为1，应另列而不冒充正式单核曲线点。mean(a/b)不等于mean(a)/mean(b)。
''')
put('04_冷启动1000项_待优化与无缝替换.md','''# CS-1000：待优化实验与无缝替换接口

**状态：待优化、未完成全量、不得作为正文已验证优势。**

快照：2026-09-26 20:07:51，北京时间；270/1000结束、237有效、33需处理、730未完成；4192次已记录候选评估。已有暂停记录表明在途任务结束。这里记录的是取回的固定快照，不承诺服务器之后状态不变。

1000=100图×5核×integrated/joint_tonight两路线；500组成对配置。预算为每项最多24调用、240秒搜索软预算、单次评估60秒、外层360秒兜底。新旧方案均从原图开始，不可用累计精选填入失败槽位。

已观察到若初始化候选都在单次时限内未完成，则总预算消耗后仍无有效方案。无有效方案不表示图不可调度。尚需区分图解析、候选生成、官方核内调度和运行器恢复开销；从已支付记录恢复父解的修补代码已存档，但尚不能写成完整成功的解决方案。

## 正文固定占位块（§8.2；不影响其他章节）

<!-- BEGIN CS-1000 -->
为检验局部联合修复在完整从头求解中的适用性，我们设置100图、1—5核、两路线共1000项对照。当前版本中该实验因初始化及评估超时相关的稳健性问题尚未完成，因此不用于支持新流程普遍优于成熟方法的结论。已有全量方案复评和成熟算法结果仍按各自实验协议报告。

【CS-1000-TABLE：待补入完整覆盖、首次成功/失败/未运行、恢复调用、有效配对数、胜平负、尾部退步、求解耗时及独立复评一致性。】

【CS-1000-RESULT：待补入同预算结果与适用范围；若仍不完整，保留未完成说明，不删掉失败行。】
<!-- END CS-1000 -->

前接句：“累计精选说明已找到方案的质量，但不等价于新算法在固定预算内的稳定生成能力。”
后接句：“因此，本文最终发布流程仍采用已经核验的成熟路线，实验性联合求解仅作为显式扩展。”若新证据足以改变此句，必须同步更改算法入口和所有对应主表，不可只替换结论。

## 后续填表字段

|字段|占位值|计算说明|
|---|---|---|
|batch_id/code_version|PENDING|修补后必须新批次；旧记录保留|
|expected/completed/success/failed/not_run|PENDING|分开计数，不以结束等同成功|
|independent_verified|PENDING|最终方案胜负两类均应核验|
|valid_pairs/wins/ties/losses|PENDING|同时给全体覆盖率，不能只给幸存者均分|
|mean_pair_time_ratio/median/worst_regression|PENDING|逐配置比值；不同核数同图不是独立图样本|
|calls/generation_seconds/solver_seconds/recovery_calls|PENDING|所有失败、恢复和生成成本纳入|
|budget_protocol_changed|PENDING|改变超时、初始化或恢复预算必须明示|

## 插入条件与操作

1. 新分支修补，保留本标签不动；记录源版本、运行环境、预算与截止。
2. 导出全部1000状态行；检查未运行/无有效方案，禁止静默补历史答案。
3. 对最终有效方案独立复评；同预算比较与延时诊断分表。
4. 更新本文件CS-1000块、摘要中的对应一句及§7.1成本表；其余论文结果不自动变更。
5. 新结果若只改善局部，写局部结论；不足以支持默认替换时保留成熟主干。
''')
put('05_作用创新与不足.md','''# 作用、创新候选与不足：落稿措辞边界

|内容|明显作用及证据|建议论文表述|不能声称/改善空间|
|---|---|---|---|
|Component/WCC强基线|保存依赖局部性，构造合法起点|作为强对照与结构路由基础|不是仅因使用WCC就构成创新；大组件易失衡|
|操作级分核与顺序构造|缓解组件粒度的并行限制|面向场景B的细粒度通信—负载折中|尚无全局最优证明；静态代价不能完全描述共享DDR|
|官方轨迹驱动修复|实际等待比纯静态估计更接近瓶颈|本文设计的场景化候选构造与复评闭环|不能把整套成熟算法的收益全归给某个算子|
|受保护成熟前缀＋少量插空|避免新候选无条件挤占强路线|预算受限条件下的保守接受与低成本补充|需要记录实际预算；候选生成也消耗时间|
|P3关键读取时窗|046同方案P2/P3揭示缓存与分配联动|缓存资源与调度时序协同的机制证据|命中率不是目标；32次FIFO联合没有刷新最好，不能写FIFO普遍有效|
|连续联合搜索|有真实结构变化与连续深入；225臂证据完整|探索性扩展及为何不晋级的分析|P1整体退步、P3收益集中；不是最终默认优势|
|局部CP-SAT/多尺度|047有独立复评小收益；085来自束搜索|有限局部代理模型的联合修复实验|CP OPTIMAL仅针对代理；未实现自由最终任务边界全局重划；模型排序有反例|
|跨核复用保底|044/083已评更快方案；083还少COPY|低成本覆盖补漏，必要工程改进|不是新理论；未评其他32项不能先宣称收益|
|真机机制研究|7类488组14640计时支持候选启发|与官方模拟分开的机制实验|192MiB真实L2≠题设1MiB FIFO；真机倍数≠赛题加速比|
|可追踪实验与复评|1500固定方案重放＋恢复、正负例齐备|可复现性与稳健性审计|工程严谨不自动等同建模首创；未完成1000必须公开|

“本文提出/设计”可以指我们构造并实现的具体流程；“首次”“最优”“显著优于所有方法”需要文献与实验额外支持，本版不提供这些强断言。创新点应聚焦场景语义、粒度选择、关键等待/关键读取候选及预算耦合；不堆砌求解器名称。

固定划分的下界只能判断该划分下的空间，不能证明跨划分全局接近最优。小收益说明当前候选和预算下边际收益下降，不能推出没有其他更好结构。
''')
print('mapped',len(rows))
