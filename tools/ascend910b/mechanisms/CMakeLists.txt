cmake_minimum_required(VERSION 3.16)
project(ascend_mechanisms LANGUAGES C CXX)
set(SOC_VERSION "Ascend910B3" CACHE STRING "Assigned SoC")
set(ASCEND_CANN_PACKAGE_PATH "$ENV{ASCEND_HOME_PATH}" CACHE PATH "CANN root")
include(${ASCEND_CANN_PACKAGE_PATH}/aarch64-linux/tikcpp/ascendc_kernel_cmake/ascendc.cmake)
ascendc_library(mechanism_kernels STATIC vector_probe.cpp)
ascendc_compile_options(mechanism_kernels PRIVATE -O2)
add_executable(mechanism_bench main.cpp)
target_link_libraries(mechanism_bench PRIVATE mechanism_kernels host_intf_pub ascendcl)
target_compile_features(mechanism_bench PRIVATE cxx_std_17)
