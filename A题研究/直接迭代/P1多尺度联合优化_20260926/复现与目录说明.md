# 复现与目录说明

先读 `本轮结果与交接.md`。本轮已经完成，不存在等待自动继续的后台批次。不要把 `今晚执行与收口安排.md` 的原定时间表误读为尚未完成清单。

## 环境

全部命令从仓库的 `A题研究/直接迭代/` 运行，保留仓库相邻的 `选题分析/A题附件/`、`A题研究/solver/` 和既有求解模块。使用Python3.12；CP-SAT可选依赖见 `requirements-p1-joint.txt`。运行器使用Unix文件锁/限时机制，支持本轮验证的macOS环境；队友Windows建议用WSL/Linux，不宣称已在原生Windows验证。

```sh
python3.12 -m venv .venv-p1-joint
.venv-p1-joint/bin/python -m pip install -r requirements-p1-joint.txt
```

CPU即可，2个并行实验；本轮硬件为Apple M5、10核CPU、16GB内存。GPU/NPU不是必需。本机已安装解释器为 `/Users/liyu/.cache/mathmodel-a/cpsat-py312/bin/python`，下面以 `python` 代称所选环境。

## 先使用已验证方案

`交付1500/selected_plans.tar.gz` 保持 `plans/p1/n5/case_047_multicore_res.json` 等包内布局。`交付1500/`另有成绩表、变更表及时间/COPY备选，来源和核验范围见该目录说明。原始旧库没有被覆盖。

新增方案可直接通过旧入口复评；例如下面只评一次已保存047方案，不搜索，也不需要原机历史路径：

```sh
python solve.py --case 47 --problem 1 --cores 5 \
  --incumbent-plan P1多尺度联合优化_20260926/run_v2/verified_plans/case_047_p1_n5.json \
  --budget 1 --seconds 60 --out /tmp/p1-047-independent-replay
```

输出目录必须未存在，每次重放使用新的目录。独立复评应得到158234周期、额外COPY3966906字节。085同理，对应872469周期、23398256字节。

## 从原图比较完整路线

```sh
python run_p1_tonight_cold_panel.py \
  --panel P1多尺度联合优化_20260926/cold_panel_plan.json \
  --out /tmp/p1-joint-new-comparison
python run_p1_tonight_cold_panel.py \
  --panel P1多尺度联合优化_20260926/cold_panel_plan.json \
  --out /tmp/p1-joint-new-comparison --verify
```

第一条会新增实验；第二条仅复评新路线优于同预算成熟路线的配置，不代表比精选库强。当前源码包含Task编号去重修复，与原六配置实验时版本不同；重新生成的候选和结果可能变化。原实验输出保留在 `cold_panel/`，修复后定向结果在 `cold_panel_noop_fix/`。复评原计划无需重新生成它。

`joint_tonight` 目前通过该成对运行器或 `cold_portfolio.run` 的显式variant调用，**不是** `solve.py` 已有的CLI方法名。成熟发布入口维持原状。`tonight` 家族每次候选生成上限20秒；完整对照每路线240秒软总预算、最多24次评测，旧新两路线均含初始化成本。

## 代码和证据索引

|文件/目录|作用|
|---|---|
|`p1_joint_frontier.py`|共同块表示、关键区域、小/大范围束搜索和最终方案代理重算|
|`p1_joint_multilevel.py`|粗化、粗细映射、解投影与边界修复|
|`p1_joint_cpsat.py`|固定局部块的核心和顺序联合求解、初解与上界|
|`run_p1_joint_tonight.py`|强父解机制实验、区域合并、独立复评接收|
|`cold_portfolio.py`|成熟组合及显式实验variant，后者仅P1|
|`run_p1_tonight_cold_panel.py`|同预算完整流程成对比较|
|`run_v1/`、`run_v2/`|27份真实候选与3项独立复评，以及所有负例|
|`cold_panel/`|六配置原始完整比较、胜例复评、逐调用记录|
|`cold_panel_noop_fix/`|只针对048的修复后回归，单独报告|
|`论文证据/图表/`|PNG、PDF、SVG和作图CSV，包含负例|
|`交付1500/`|单独完整方案包，不覆盖原库|

旧实验记录保留原机绝对路径用于溯源。跨电脑读取历史诊断脚本可能需要替换路径前缀；跨电脑复评导出的方案、从原图运行新的成对实验不依赖这些历史路径。温启动实验的 `prepare` 依赖前轮接力记录，不应把它当作任意原图的通用入口。

`实验原始记录.tar.gz` 打包本轮四个批次及前轮接力记录，供Git交接，避免依赖被忽略的本机缓存；解包位置为 `A题研究/直接迭代/`。解包前应使用新克隆或空目录，避免覆盖接手者自己同名的实验。归档没有附带环境、账号或SSH配置。本轮312次评测与前轮80次在各自目录中，统计时不要混合。

测试命令：

```sh
python -m unittest test_p1_joint_frontier test_p1_joint_multilevel \
  test_p1_joint_cpsat test_p1_joint_tonight test_p1_tonight_portfolio test_cold_portfolio -q
```

本轮59项通过。作图使用Matplotlib和Pillow；它们不是求解器依赖。原图表及数据均已输出，阅读图表不需要额外安装。
