# 联合整合阶段记录

用户批准按《对比与下一步建议》执行。所有研究都在 `codex/joint-integration-v3`，main保持原状。

## 回滚点

重点版本标签：`milestone/pre-integration-20260925`，提交 `238aafecf447488e1397467ddebe4d994477c899`。
本地和GitHub标签都已建立，远端另有同名milestone分支。标签指向整合开始前的源码、历史成果和审计报告。

需要恢复该版本时，可新建恢复分支，不覆盖当前成果：

```sh
git switch -c restore/pre-integration refs/tags/milestone/pre-integration-20260925
```

## 合并记录

`14b91b6`是实际双父合并提交，保留队友`8f3837f`与我方`238aafe`的历史。双方同名模块分别保存：

- 队友重Task细分和全局重分配：`p1_joint_regions.py`。
- 我方原局部迁移与联合合并：`p1_local_regions.py`；旧调用及测试已更新导入。
- 队友实验驱动：`run_region_panel.py`。
- 我方旧实验驱动：`run_local_region_panel.py`。

CLI同时保留既有v2、task_iterative、region_joint和队友新增方法。合并后99项旧测试通过。

## 累计并集已经完成

1500份计划均通过原始算子覆盖、DAG和核数检查。相对队友第十三轮接收的111项全部独立官方复评一致，111次新调用、零失败、6个进程约167.92秒。其余1389项继承队友成功记录；不是这次全部重跑。

五核100图平均原单核加速比为P1 **3.78370540**、P2 **4.53546717**、P3 **4.65487832**。完整计划、来源、哈希和曲线数据见`累计并集/`。

## 整合候选控制

`2362dd7`冻结首版整合代码、实验协议和已复评并集。新增`joint_solver.py`，以原强主干最多12次调用为受保护前缀，剩余额度用于互补候选。完整分量父方案用于WCC，旧父方案队列保留两次收费调用的续跑配额；改善至少1%可追加一次新父方案，而不是把旧队列丢掉。

P1尾部包括结构候选、沿完工链选择跨旧Task区域的重划、队友joint；P2/P3按当前大分量或输入/溢出特征选择结构、region、data、legacy及WCC候选。不存在case编号专用规则。

新增四项测试覆盖队列续跑、耗尽交接、精确保留字典顺序和预算参数检查，合计103项通过。joint目前是显式实验选项，没有晋升默认。

## 薄弱图诊断

以下对累计并集的P1五核方案额外做4次官方复评，作为诊断，不搜索新候选。

|图|周期|Task数|原始计算依赖路径/周期|最忙核单管线忙碌/周期|固定方案边界DDR下界/周期|
|---|---:|---:|---:|---:|---:|
|064|15177|120|23.25%|34.14%|29.80%|
|044|96349|2|7.96%|77.22%|19.85%|
|069|15487|52|14.84%|36.81%|75.19%|
|047|190683|533|10.48%|45.16%|34.63%|

064的固定Task方案必要下界13688，047为163200，分别占当前时间约90.2%、85.6%。在这些既定Task与分配下，仅调整执行细节的空间有限；需要改变导致整体等待的结构。
044固定分核单管线工作量较高，原始依赖路径却短，重划和重分核仍有研究理由；这并不保证通信开销能抵消。
069当前边界DDR必要下界11644，提示粒度碎化和搬运代价应优先关注，不能将统一细切作为答案。
上述下界都不是全局最优界，不可据此断言剩余可实现提升。

本组额外4次复评与主对照前段有运行重叠。墙钟数据仅为实际运行观察，不用来宣称纯程序加速。

## 已登记的决策

完整协议见`执行协议.json`。共216实验臂：12张开发图、12张验证图的三场景五核，以及4张图三场景2—4核；每臂统一B16/T180、单次45秒、6进程、全新评测目录。B8/B12/B16列是同一次B16运行的实际前缀，不假装额外执行了独立低预算算法。

验证图从未参与本次控制规则调整，但可能被历史实验看过，不称盲测。每场景要求验证平均降时至少0.5%、至少3胜、最大退步不超过3%，低核均值不退步且有效性通过。三场景都通过才启动最终1500从头全量；失败则保留强主干和有效精选成果，不事后修改门槛。
