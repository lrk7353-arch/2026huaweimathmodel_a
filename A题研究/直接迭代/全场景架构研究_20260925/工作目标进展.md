# 三批次目标进展

目标范围已按用户最新指示变更：再尝试两轮较为冒进的改进，若没有明显改进就结束。A/B成果和C宽筛保留；不再要求继续跑完已中止的1500配置。不能把新结束条件写成旧全量要求已经完成。

- A：已实现、完成首版与修复回归。首版411次评估；修复212次评估。全部负例保存。见批次A目录。
- B：已实现并完成54槽同预算对照，574次评估；另98次跨核接口检查、4次跨场景/跨核种子迁移。补充051修复先12次、再3次，最新290405。见批次B目录。
- C：宽筛完成300配置、2054次调用，298配置有效；P1的014/087旧版超时记录保留，最终版已通过粒度保底修复验证。宽筛三题相对历史累计库分别20/25/29个严格胜例，但冷启动整体均值仍落后，不能宣布全局升级。见批次C_100图宽筛。
- C最终运行：按新指示中止，保留98个有效目标配置和6个单核参考复算（其中5个超时），共558次调用。成熟精修8配置、显式热启动12配置均有效。部分全量数据不用于宣称完整统一版本成绩。
- 最后两轮：第一轮30配置250调用，零降时胜例；第二轮30配置212调用，13个严格胜例，三题合计平均降时0.189569%，无任何配置降时达到5%。未达到预先登记的平均3%且至少3张图降时5%的标准，按指示收尾，不开第三轮。
- 固定原单核参考值的复算超时不影响目标配置合法性：超时项明确引用原有累计账本分母，保留失败费用；成功复算项全部与同一账本逐项核对。所有1500个P1/P2/P3目标配置仍须本轮官方成功，禁止用历史方案填入冷启动成绩。

收尾交付：133份新增累计精选已全部独立官方复评，并按15个问题/核数组合分包；这15包只包含133份新增方案，不是1500份全量方案。累计账本、完整调用账本、源码与负例已归档。本阶段可核对官方调用4721次（搜索/基线4583、复评138），其中88次超时。累计库五核平均加速比分别为P1 3.66534943、P2 4.38842725、P3 4.48257935，均使用原始单核分母；不能冒充统一冷启动成绩。详见《最终收尾报告》。

模型只用9张开发图的结构特征，不使用图名查解。源方案跨场景迁移必须显式记上游成本。官方原图、配置、评测器保持不变。

本地分支codex/unified-scenario-strategy-v2；GitHub草稿PR #3承接本次演进。每个冻结实验目录保留manifest、逐候选记录、真实调用、超时/失败和官方结果哈希。
