# 批次B：联合搜索、效果与进入全池前的修复

实现完整候选解析评分、最多6份多样父方案、跨旧Task边界区域破坏/修复、另一父方案核心归属重组、保留未合并及合并版本；P3基于当前获胜父方案的官方轨迹追加FIFO读序候选。父方案可来自新的区域分配，读序不固定在初始映射上。

评分由可解释结构特征和近邻校准构成，只决定官方调用顺序。训练仅使用9张开发图的已发生测量，模型不读取case编号；数据源编号只用于追溯。所有候选统一记调用、生成耗时、评估耗时、失败和超时。

54个同预算槽位全部有有效解，574次官方调用。P1为5胜4平、平均降时15.537%；P2为2胜5平2负、平均退步0.788%；P3为4胜4平1负、平均降时0.895%。口径：9张开发图、五核、最多12次调用/180秒、8进程；不是全量成绩。大图受到CPU争用影响，最大调用预算相同不代表每格实际调用数相同。

与累计库比较，P1有047/048/085三份新解；P2/P3各有025的小改善。P3的085/047/043相对本轮冷启动基线获胜，但仍未超过原累计库，不能把这些收益混为累计新增成果。

补充接口验证：064/071两图的三题、1—4核共24槽，另2个官方整图单核基准，全部成功，共98次官方调用。跨核/跨场景迁移也已验证：071的P2四核求解结果作为P3五核的显式种子，目标新增4次调用；上游4次求解调用和耗时保留在upstream_searches，不冒充免费冷启动。

全池前修复：

- 保留第二个不同的成熟P2/P3分配起点，避免稀疏校准把相近通信权重排错。
- P1对工作较重的主导连通区域同时保留严格分支和适度融合；严格分支后直接生成完整分配+合并候选。051修复验证中，仅3次官方调用即得到290405，优于历史293518。
- 对完整方案增加必要计算/Task屏障下界，能证明无法超过已验证最优时才剪枝；解析预测仍不当作下界。每次成功官方测量自动断言下界安全。
- 已达到计算必要下界98%的方案最多先探两次，再停止重搜索。
- 大图单独限制同时执行数，其他槽位供中小图使用；按实测吞吐和内存调节总进程数。完整源代码快照随实验保存。

进入C批：先100图三题五核宽筛，再冻结最终规则并完成三题×100图×1—5核1500配置；另重算100个官方整图单核基准。新统一算法成绩与累计精选库分别导出，P3必须报告同核数无L2/有L2比较。
