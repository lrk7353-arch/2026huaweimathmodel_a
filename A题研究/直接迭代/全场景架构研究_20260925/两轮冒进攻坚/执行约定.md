# 最后两轮攻坚

用户最新指示：再尝试两轮较为冒进的改进，若无明显改进则结束目标。本约定替代继续完成1500配置全量的要求；已完成记录全部保留，不将中断全量宣称为完成。

第一轮：不受旧Task边界限制的张量所有权全局搜索。混合已有最优分配与从输出倒推的核心分配，用成块迁移、交换及允许代理暂时变差的退火搜索平衡M/V与张量通信，再以同核可执行前沿重建无环子图。不同通信权重与粒度形成8个完整候选。

第二轮：根据第一轮失败机制再改变候选，最多8个新官方候选/配置；禁止只是追加同一候选的次数。第二轮起点保留第一轮实际获胜方案，累计改进仍以冻结原起点计算。

面板：025、043、047、048、051、064、068、071、075、085，P1/P2/P3各五核，共30配置。025保护接近计算下界的优势图，其余覆盖既有瓶颈。它是攻坚开发面板，不是全池/盲测。每个起点取冻结时所有已有本地记录和历史账本中的最优方案；每轮先支付一次官方复评以验证起点，最多9次总调用、150秒软预算、新候选单次35秒。初始复评最高90秒。

预先定义“明显改进”：30个配置保留最优后的平均降时至少3%，且至少3张不同图出现至少5%的降时。三题分别报告，单题局部收益不能宣称三题普遍跃升。全部新胜例另行独立复评才纳入精选库。

原图、操作、硬件配置和官方评测器不变。原有成熟求解器和累计方案保留，探索模块只作为可选插件，不替换默认。即使两轮没有达到阈值，也保存源码、负例、调用费用、结果和可以复用的单项收益，然后结束当前目标。
