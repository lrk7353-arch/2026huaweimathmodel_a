# C批最终冻结配置

目标为100图×P1/P2/P3×1—5核共1500配置，另尝试复算100个官方原单核基线。统一入口全部从头求解，不隐式载入累计方案；显式热启动与精选库单独记录。

- 种子17；每配置最多16次官方调用；软总时间240秒；单次官方进程90秒。
- 小图达到计算必要下界的98%后可提前停止；所有失败调用计入预算。
- 使用A/B开发9图产生的518条冻结结构经验。100图宽筛结果不重新拟合经验模型；模型不使用图名或编号。
- 进程上限12，按剩余图规模和可用内存调整；大于4 MiB的输入首段同时最多4个，续跑调整为最多6个。异质任务不再用相邻窗口吞吐变化判断并发优劣。宽筛原先的大图并发上限2及错误降档造成尾部串行，不能据此宣称12并发的实测速度。
- 与宽筛首版相比，补入成熟WCC交错与官方轨迹迁移，并对P1大Task先给出有界粒度起点；超过20000操作时首个保底候选上限256操作，其余大Task上限1024。超时且尚无成功解时继续缩小保底粒度。该规则用于有效返回，不替代分支重划。
- 已核验：14项结构/预算/既有Task回归通过；成熟精修接口8配置有效；显式热启动12配置有效，初始复评计费；014/087从无效恢复有效。014进一步以256粒度在两次调用内得到3678253周期。

宽筛的已完成部分用于发现并修复上述问题；其剩余大图已以原冻结版本完成，原始负例保留，共300配置、2054次调用、298配置有效。最终全量使用单独Git工作区和输出目录，与宽筛收尾可共存，调度器按实时内存约束控制资源。若最终出现无效目标配置，将保留失败账本并按同一公开规则修复，不把缺失目标配置排除后报告均值。

验收导出实际单核求解时间、P1/P2单核锚点为1的曲线、P3同核P2/P3时间比、附加搬运、命中率、全部调用及超时成本。最终全量不是额外的盲测；源码默认入口仍保留legacy，是否替换默认由完整结果决定。

执行段追补：最终全量首段保留18个成功配置和69次调用；只修正调度程序后继续，搜索模块、候选、图、配置、种子和调用预算逐项比对不变。宽筛也单独保留旧版求解代码，在独立工作区续跑其剩余13配置；其287个既有配置和1958次调用全部保留。两轮均保留原始清单、摘要哈希及中断记账，不改写负例，不重复计费已完成配置。源码冻结与执行调度修复分开说明。

原单核分母处理：若整图原单核复算超过240秒，则不追加重复长任务，显式引用`闭环验证_20260925/下一阶段攻坚/重划实证_v1/累计1500配置成绩.csv`中已冻结的`original_singlecore`，账本SHA256为`73163206f09e696bcab5258ac47a73f12edb50f20a8f6d5714308b17cdb21c73`。每个成功复算值必须与历史值完全一致，否则终止导出；超时项标记为本轮未独立复算并保留全部费用。此规则只针对曲线分母，不允许替换任何P1/P2/P3目标方案。最终核验列出成功复算数量和历史来源清单。
