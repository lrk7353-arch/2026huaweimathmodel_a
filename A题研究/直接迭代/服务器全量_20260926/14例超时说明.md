# 1500方案首轮复评中的14例超时

2026-09-26 19:07，首轮1500项运行结束：1486项官方复评成功且耗时/COPY与原表一致；14项全部为 `timeout: worker exceeded 120 seconds`，没有出现成绩不匹配、非法方案或容量错误。P2和P3各500项全部通过；14项都属于P1。

|图|超时核数|
|---|---|
|016|1|
|025|1、2、3|
|041|5|
|054|2、3、4|
|058|3、4、5|
|076|5|
|087|3、4|

直接原因是首轮统一设置的120秒单次复评上限未覆盖这些慢案例。原测速48次只覆盖其他代表配置，没有覆盖这些尾部案例。这个时限设置的覆盖不足属于实验安排问题；不能把超时直接解释成方案错误。

日志显示超时时停留在官方 `evaluate` 阶段，没有stdout/stderr异常；超时记录进程内存约204—368MiB，容器内存失败计数为0。CPU累计限流记录很少，不能据此把原因归结为12并发必然过载。

结构检查显示慢方案多含大Task：016/n1单Task有17995个计算操作；054/n2及n4最大Task15390；087/n3最大Task14517。同图的更细Task方案往往明显更快完成评测。官方P1事件模拟中的任务完成检查反复构造并遍历Task操作列表，存在随任务大小放大的Python计算开销。这是有源码和结构证据的性能解释，但未做函数级剖析，不能断言是全部耗时的唯一来源。模拟器计算成绩的墙钟时间与方案的模拟makespan不是同一指标。

处理：只对14个超时的原方案做独立补跑，4并发、单次600秒，外层630秒；不改官方源码、输入图或计划。原1500次账本及14次失败记录保留，补跑另记调用次数和结果。补跑通过后检查原始输入、计划有序编码、预期耗时及COPY一致；全部通过才恢复后续冷启动搜索，原截止22:46仍保留。

补跑结果以服务器 `full_run/replay_timeout_retry/progress.json`、`replay_reconciliation.json` 为准。进度工具增加“原首轮记录”和“补跑后的已验证数量”两种显示；不把1500+14次调用冒充一次1500项全通过。
