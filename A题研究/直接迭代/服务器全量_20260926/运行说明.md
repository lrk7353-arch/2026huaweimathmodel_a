# 服务器全量实验：2026-09-26

正式批次2026-09-26 18:46:20启动，服务器调度PID 81483。截止时间22:46:20（北京时间）；不启动预计无法在截止前结束的新任务。尚未完成的项目保留为未完成，不按完成统计。

## 范围与顺序

1. **1500份现有精选方案全量官方复评**：100图×P1/P2/P3×1—5核。核对耗时与额外COPY是否与交付表一致，保存模拟缓存等完整指标。该阶段有不一致或未完成则暂停后续搜索，留待核查。
2. **P1全量从头求解对照**：100图×1—5核×成熟integrated/实验joint_tonight，共1000个独立运行任务、500组成对配置。每任务B24、240秒软预算、每次官方评测60秒，外层360秒兜底。所有初始化、候选和失败计入；不注入历史精选解。
3. **两路线最终方案独立复评**：最多1000份，胜例、负例均复评；核对全部确定性指标，排除主机进程RSS。自动形成paired_results.csv和comparison_summary.json，失败/未复评项目不当作有效比较。

P2/P3本次做全量交付复评，不重新搜索。1000个P1任务不是1000次官方调用，最大候选调用数为24000；实际次数以账本为准。全流程的3500个任务包括不同性质的任务，不能相加当成3500份新方案。

## 已完成的部署与测速

服务器ARM Linux，Python3.11.4；独立venv安装ortools9.15.6755。16个可见逻辑CPU，容器CPU配额约13核；12并发任务，每个CP求解器1线程，BLAS/OpenMP限制1线程。未修改cannbot项目、CANN环境或官方评测器。

|并发|相同16份代表方案的复评墙钟|匹配情况|
|---|---:|---:|
|4|55.534秒|16/16|
|8|43.351秒|16/16|
|12|42.581秒|16/16|

单次短测不能证明全图最佳并发度；12是本次测试中最快的配置，增益小于从4到8。正式任务采用12，并记录实际耗时与异常。测速48次复评不混进正式1500份完成率。

另对004图的1核/5核各两路线进行了完整冒烟检查：4个搜索共40次评测、4次最终复评，全部成功；断点恢复不增加评测。原有59项测试和新增4项运行器测试均在服务器通过。新增测试覆盖已完成不重付费、中断不重置预算、截止不伪装完成、完成顺序不影响恢复。

## 目录与查看进度

服务器根目录：`/mnt/workspace/mathmodel_a_full_20260926/`

- `project/`：本轮独立代码、官方数据与输入交付方案。
- `venv/`：独立Python环境。
- `full_run/replay/`：1500方案复评。
- `full_run/cold/`：1000个P1从头搜索。
- `full_run/cold_verification/`：两路线最终方案复评。
- `full_run.log`、`full_run.pid`：后台调度日志与PID。
- `bench4/bench8/bench12/`、`cold_smoke/`：测速与部署校验，不计入正式进度。

本机双击上级目录的 `查看服务器全量进度.command` 读取当前状态；命令使用VS Code已经建立的SSH主机别名。SSH通道断开只影响本机查询，已通过nohup启动的服务器进程独立运行；必须保持云实例本身运行。文件名相同不代表数据已取回，本机正式结果需后续从服务器同步。

在服务器终端也可以运行：

```sh
/mnt/workspace/mathmodel_a_full_20260926/venv/bin/python \
  /mnt/workspace/mathmodel_a_full_20260926/project/A题研究/直接迭代/show_server_full.py \
  --root /mnt/workspace/mathmodel_a_full_20260926/full_run \
  --pid-file /mnt/workspace/mathmodel_a_full_20260926/full_run.pid
```

每项结果独立原子保存。相同运行目录会跳过已结束任务；已有付费但中断的任务标为异常，不擅自给它重新领取24次预算；有存活旧进程时拒绝重叠恢复。方法、并发、配置或输入计划改变必须新建批次。截止在首次启动时固定，恢复不自动延长。

本轮不会自动覆盖旧精选库、替换默认求解器或合并Git分支。全量完成后根据已复评结果比较时间、COPY与模拟内存，再决定采用范围。
