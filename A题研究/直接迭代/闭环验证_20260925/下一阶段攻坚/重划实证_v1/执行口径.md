# P1重划攻坚：执行口径

开发轮固定043/4核、051/5核、047/5核、075/5核、064/5核；每格最多8次官方调用，180秒总时间，60秒单次，30秒构造与合并。2个进程。程序不读取图名选择策略，seed=17仅用于复用旧候选；新规则确定性。

因新划分没有匹配的官方轨迹，旧按观测时长分配改为共同的解析式EFT分配器。这样“旧计数拆分→新划分→新分配→安全合并”的比较不需要免费预评测。第一轮代码提交f5082cc；manifest记录源文件哈希；全部负例保留。

分支路线在分叉和汇合处切断链，通信路线优先把已就绪且共享大张量的操作收在一起。都按M/V周期控制大小。区域最多8192操作，完整旧Task逐步扩邻；候选完成后检查全图依赖和区域外核心顺序。新分配仅实现宽度4的核心选择束搜索，尚未实现任意局部交换、多父搜索或精确共享DDR模型，不能把计划中所有设想当成已实现。

第一轮两图改善超过5%，且原最大Task计算出现更多核心同时运行、完成显著提前，达到开发门槛。下一步严格使用预选085/048/056/068的3、5核。从头原portfolio、相同8次前缀+旧闭环、相同前缀+新重划三路各12次；新路4次余额依次留给旧精修、分支重划、通信重划、合并/多样候选。必要下界仅剪完整候选，代理只排序。原默认不变。

转移门槛：至少两张不同图优于原portfolio；平均加速比增量为正；各核数平均降时非负；单格退步不超过0.5%；无超时和非法解。也单列与旧闭环对比。通过后才进入冻结推广；否则停止扩全量，保留可验证的定向成果。

独立复评计入开发的第二轮调用，不计为免费校验。历史累计成绩只表示方案库，不表示统一算法从头等预算成绩。
