**P1迭代闭环的冻结验证**

基于上游18e0957。代码提交ec4f9bdbed55c156d57817a0ffa8bd9758a227a8，预登记之后才开始官方实验。原题、官方代码、100张图和所有跟踪的Python源码均记录SHA256；执行器启动前核对这些哈希。

选择过程不读取新实验成绩：排除016/062/063/100，在剩余96图中按计算操作数分三组，每组选择结构特征中心及与其最不同的一张。结构特征包含连通分量、最大分量占比、五核Task数及粒度。完整特征、算法规则和种子见预登记.json及结构特征与入选.csv。

| 图 | 计算操作数 | 核数 |
|---|---:|---|
| 032 | 827 | 2、3 |
| 069 | 1032 | 4、5 |
| 074 | 3609 | 2、4 |
| 002 | 1798 | 2、5 |
| 043 | 9309 | 3、4 |
| 025 | 18662 | 3、5 |

每个核数出现3次，共12配置，两方法都从对应的第六轮方案开始。A、B共用一个控制器和候选工厂，仅切换接受后是否刷新候选。起点复评计入各自8次逻辑调用及180秒总预算，单次评测60秒。起点物理共用一次，候选缓存各自为空；2个工作进程、种子17，方法执行顺序交替。

```sh
python3 -B -m unittest discover -s A题研究/直接迭代 -p 'test*.py'
python3 -B A题研究/直接迭代/run_iterative_panel.py \
  --protocol A题研究/直接迭代/闭环验证_20260925/预登记.json \
  --out my_runs/iterative_holdout_b8_v1
```

冻结前24项测试通过，包括“贪心刷新可能丢掉旧父方案更好候选”的反例。默认求解模式保持不变，P2/P3及官方评测代码未修改。

晋级要求：至少两张不同图获益、各核数组平均时间改善不为负、平均参照加速比差为正、最大单配置退步不超过0.5%，且无新增错误或超时。预算12的追加只在全组至少两个迭代配置耗尽调用，且其中至少一个在最后两个候选内接受了第二代及以后改进时触发；追加时仍运行完整12配置。条件在预登记中冻结，不按输赢挑选追加图。

这些是工程筛选规则，不是统计显著性保证。当前图集只称“未用于本控制器调参的保留回归组”，不是未见过的新数据集。结果是否支持从头整合，要等本批完成后根据上述规则判断。

原始过程保存在my_runs，公开记录选用紧凑汇总、全部正负例、输入哈希、获益方案及独立复评证据。Git分支用于审阅，main保持队友当前版本。
