**当前成绩与下一阶段计划复审**

2026-09-25。核查时GitHub最新提交仍为[18e0957475b20e9a3af52ab335dccb2611beefc9](https://github.com/lrk7353-arch/2026huaweimathmodel_a/commit/18e0957475b20e9a3af52ab335dccb2611beefc9)，即第六轮。以下补充此前六步计划，不改变已封存的交接包。

Material Passport：模式为计划复审与成绩核算；来源为原题摘录、第六轮1500行成绩、源码及本地四份已复评方案；Verification Status为ANALYZED（独立重算统计，本次新增官方评测0次）。四份本地方案的官方复评沿用上一轮证据。这里的“合并”是统计上的纳入，未表示已推送GitHub或已跑完新的统一算法。

**一、现在的成绩具体是多少**

主表按每图原始整图官方单核基准除以多核Makespan，再对100图取算术平均。不能用总单核时间除以总多核时间，也不能先求两组均值再用均值之比替代逐图比值。

| 核数 | P1，第六轮 | P1，纳入本地四份方案 | P2 | P3，相对原始单核参照 |
|---|---:|---:|---:|---:|
| 2 | 1.784524 | 1.784524 | 2.042011 | 2.048350 |
| 3 | 2.425837 | 2.425837 | 2.897896 | 2.914772 |
| 4 | 3.031382 | 3.031382 | 3.606767 | 3.641776 |
| 5 | 3.576101 | **3.587639** | **4.334518** | **4.428466** |

原题规定P1/P2曲线的单核点为1；它使用原始整图核内调度基准，不用优化后的单核方案重新定义基准。表中P3同样相对这个公共原始单核参照，仅用于横向展示，不能代替第三问要求的同核数无L2对照。

本地新增结果使P1五核均值增加0.011537638。016、062、063分别贡献约0.000590030、0.005900955、0.005046653；100的时间不变，所以只改善次要搬运指标。单图16.31%的降时并不等于100图平均加速比提高16.31%。

| 五核当前累计成果 | 平均加速比 | 中位数 | 加速比至少4的图数 | 100图Makespan之和（周期） | 100图额外COPY之和（字节） |
|---|---:|---:|---:|---:|---:|
| P1，含本地四份 | 3.587639 | 4.163240 | 55/100 | 78251960 | 1762428050 |
| P2 | 4.334518 | 4.640962 | 63/100 | 62964333 | 1003965076 |
| P3 | 4.428466 | 4.752114 | 67/100 | 62340005 | 1036618282 |

时间总和和搬运总和只作描述，不是题目规定的平均加速比，更不是官方总分。个别图的加速比超过核数并不自动说明数据错误：参照是指定的启发式单核调度，切分还会改变核内排序和缓存换入换出；核数本身不能直接当作这个参照指标的严格上界。

P1中位数已经超过4，均值仍较低，说明部分图拖累明显。例如五核case_051为1.000、047约1.017、048约1.050。低加速比不等于一定有可兑现的优化空间，后续要结合必要下界、依赖和资源等待判断。P1与P2约束不同，不能把两者的全部差距解释成算法质量差距。

**P3还必须看同核数下的无L2/有L2比值。** 当前分别采用累计最优P2、P3方案，逐图计算T(P2)/T(P3)：

| 核数 | 平均比值 | 平均逐图时间下降 | P3更快/持平/更慢 |
|---|---:|---:|---:|
| 1 | 1.008622 | 0.692237% | 25/75/0 |
| 2 | 1.002946 | 0.284650% | 53/45/2 |
| 3 | 1.006086 | 0.589436% | 55/45/0 |
| 4 | 1.011427 | 1.074153% | 60/39/1 |
| 5 | **1.026398** | **2.404112%** | **70/30/0** |

“平均加速比增加2.6398%”与“平均时间下降2.4041%”是不同统计量，不能相互替换。这些数字比较两个场景各自选出的方案，混合了配置与方案差异，不能全归因于L2硬件。要解释缓存本身的贡献，仍须使用相同方案在两种配置下的交叉评测；当前18图机制实验提供局部证据，不是全100图的固定方案归因。

**关于得分：原题1.4及问题1～3给出了Makespan、额外搬运、Cache命中率和加速比的定义，但在已提供题目与官方代码中未找到将它们合成为百分制或其他标量总分的公式。** 因此当前可核实的成绩就是上述指标，官方总分和排名未知。不能用加速比除以核数乘100，或把P1/P2/P3平均，冒充官方得分。若把社区的“五核平均4.0”作为交流参考，P2/P3已超过，P1还差约0.412361；这不是题目公布的评分线。

单核输出另有一项应核对：第六轮P1的100份单核方案中，38份的时间高于原始整图单核参照，例如044为162836对154407、062为2977362对2854809，所选方案分别有2和20个Task。Task边界可能造成额外代价，但本次未逐个复评整图候选，不能直接断言可用零成本回退解决。后续要核查单核输出与原题基准的对应关系；正文单核点仍按题意定义为1，不能据此掩盖附录中的实际方案时间。

**二、原计划可以推进，但要改成分阶段、有退出条件的计划**

整体目标仍正确：在题目允许的子图划分、核心映射、同核顺序三个变量上联合优化。当前可直接进入小面板验证；从头整合、区域搜索扩展、P3预算策略、全量1500应分别经过证据门槛，不能视为已经证明有益的必做改造。

复审发现五处需要补充：

1. **第1步大部分已经完成。** 四份方案、补丁和交接包已经准备好。队友是否已经接收尚未核实，本地验证不需要等待交接完成。
2. **闭环对照必须只有一个主要变化。** 不能直接把旧CLI的tasks模式与新task_iterative比较后，把所有差异归给闭环：新入口还使用portfolio的细粒度合并候选选择。应使用同一候选生成器、合并上限、排序、种子、下界、超时及起点；A一次生成，B接受后重新生成。上一轮G对照已做了这种候选对齐，下一轮继续保持。
3. **单父方案贪心刷新存在风险。** 当前代码在接受第一个改进后就放弃旧候选列表的剩余部分。它可能沿小收益分支持续花费预算，错过原父方案的更大改进。保留输入最好方案只能防止比输入更差，不能保证胜过同预算的A。这是要检验的假设，不能提前宣布闭环一定更好。
4. **从头模式的后段预算可能被用完。** 现portfolio使用reserve=min(4,B//3)，前段上限为B-reserve，随后张量候选还能用2次。若阶段都用满，B=8时Task精修可能没有调用，B=12或16时只剩2次。只把新循环接进去，再比较8/12/16，未必真正测到了迭代能力。必须单独记录各阶段用量，安排明确的精修额度。
5. **原计划漏了P2的持续回归。** P1的Task合并收益不能原样搬到P2。凡是共享的核心分配或候选编码发生变化，都要在P2做小回归；P2还应单独检查负载、跨核同步、DDR争用和缓存驻留，而不是把注意力全部交给P1/P3。

**三、下一批实验的具体安排**

第一阶段只研究“接受后再生成是否值得”。固定上游18e0957、本地实现版本和官方文件哈希，不在同一批次中追随上游变化。第六轮方案作为共同起点；016/062/063/100继续放开发回归组，不能用来宣传未见图效果。

预登记一个小面板：从其余图中按原图计算规模、连通结构和已有Task粒度挑6张结构不同的图，先冻结图号和选择记录，再产生任何新闭环结果。每图测试两个核数，共12个图×核数配置，安排2/3/4/5核各出现3次。原100图是有限的开发语料，这里称“未用于本闭环调参的保留回归组”，不宣称从未被任何旧方法看过的独立测试集。

A、B分别从同一个第六轮方案开始，先用总调用上限8；含起点官方复评，失败也计一次，使用相同180秒总预算、60秒单次上限及种子17。起点可以物理共用一次，但每个方法的逻辑预算都扣1次；两组候选使用独立空缓存。并发先用2个工作进程，时间仅用于本机成本记录。

这一批为12配置×2方法，最多192次逻辑调用；若起点复评共用，则最多180次实际新调用，去重、剪枝和提前穷尽还会减少调用。它不是把每个小改动都拿去跑1500组合。

运行前固定以下报告和晋级条件，属于工程筛选标准，不是假设检验的显著性门槛。下面加速比差定义为S(B)-S(A)，时间改善定义为(T(A)-T(B))/T(A)，正值表示新闭环更好：

- 所有返回方案合法；无不安全下界、遗漏算子或新增评测异常。失败与超时必须保留，不能从汇总中删掉。
- 相对A，逐配置加速比差和时间改善都报告；按核数分别汇总，不能用少数五核大增益掩盖低核退步。记录每次调用后的最好成绩，区分更好的搜索与更多实际调用。
- 进入从头整合小试的最低信号：至少两张不同图的Makespan严格获益；各核数组的平均时间改善不为负；全组平均加速比差为正；没有单配置超过0.5%的时间退步。阈值在运行前固定，若未通过，仅保留为定向精修并分析失败原因。
- 8次预算若不足，是否开展12次实验按全组的调用耗尽与候选穷尽记录决定，不能只给赢家加预算。开展时对原12配置整体运行并单独报告；不在看到输赢后更换图组。

第二阶段才考虑从头整合。先让“单轮Task”与“迭代Task”共享完全相同的前段生成和前段预算，再比较后段。一个可检验的初始分配是B=8/12/16分别使用前段5/8/10次、精修3/4/6次；张量候选算在前段，不能再挤占声明保留的后段。它不是已测得的最优分配，应作为预算分配实验明确标注。另保留原portfolio作为完整基线，区分预算重分配的收益与迭代本身的收益。

若只改刷新机制就失利，先检查旧父方案剩余候选被丢弃、轨迹时长失真、精修预算不足这三项。可以再测试保留少量父方案候选的队列，但要另起版本，不能把新旧结果混起来称为同一个冻结算法。

**四、其他路线如何安排**

- P1作为当前主线。闭环小面板与低成本瓶颈诊断可以同时推进，诊断不必等待从头整合完成；新闭环尚不能承担把均值提升到4.0的全部期待。局部区域原型暂不普遍扩量；先在强Task路线停滞的图上，用必要下界和官方等待诊断选择目标，再研究区域交换、不同粒度修复和多父方案。重点不是只挑加速比最低的图，而是找有可兑现余量、对100图均值有贡献的改动。
- P2保留现有强基线，贯穿共享代码的回归。优先区分分配不均、跨核等待、DDR与容量压力，不能用P1的Task数下降作为成功证据。
- P3按瓶颈选择候选仍值得研究，但属于另一项独立假设，不是P1闭环验证的前置条件。继续保留固定方案的无L2/有L2交叉对照；在冻结规则前，第六轮18图只能算开发证据。
- 统一版本1500放在最后。冻结可执行算法、场景参数、核心数、预算、种子和失败回退；报告统一版本的新结果，同时单列累计精选方案库。若某实验没有通过晋级门槛，就不把它强行纳入统一算法。

本次复审的执行结论是：**可以推进，当前应推进到候选对齐的小面板验证；还没有证据支持立即替换默认算法或启动新版本1500全量。** 本次工作完成了成绩重算和计划收紧，没有启动这批新的官方实验。

统计方法检查11/11：分组反转（按核数分别报告）、群体推个体（保留逐图）、选择偏差（累计最好与新算法分开）、碰撞变量偏差（未做条件因果估计）、基率忽略（全部100图）、均值回归（确定性对照、不凭低分选收益）、幸存者偏差（失败不可删除）、多处搜索（不做显著性宣称）、分析自由度（下一批先冻结）、相关性冒充因果（P3跨方案不等于纯缓存）、反向因果（不从命中率直接推出降时）。无p值、置信区间或总体泛化宣称。

复现本次统计：在questionA目录运行 `python3 -B 下一阶段计划复审_20260925/recalculate_scores.py --repo 联合优化工作区 --out 下一阶段计划复审_20260925`。脚本仅读取成绩和已有复评记录、重算CSV/JSON，不调用官方模拟器。输入哈希、1500行覆盖检查、四份覆盖结果检查、12组仓库均值交叉核对均保存在核算记录及脚本中。
