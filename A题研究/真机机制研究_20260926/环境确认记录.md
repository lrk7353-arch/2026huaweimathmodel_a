# 远端环境确认

日期：2026-09-26。先由用户终端截图确认，随后已通过本地VSCode官方SSH通道进入同一实例，完成运行时查询和小规模NPU数值验证。下表保留最初截图记录；后文列出直接执行证据。

|观测|结果|
|---|---|
|npu-smi info|成功列出设备|
|型号|910B3|
|管理设备编号 / Chip|3 / 0|
|健康状态|OK|
|npu-smi显示版本|25.5.0（不要当作CANN版本）|
|AI Core利用率|截图时0%|
|进程列表|截图时No running processes found in NPU 3|
|HBM|3202 / 65536 MB|
|python3|/opt/buildtools/Python-3.11.4/bin/python3|
|bisheng|/home/developer/Ascend/cann-9.0.0/bin/bisheng|
|ccec|/home/developer/Ascend/cann-9.0.0/bin/ccec|
|msprof|/home/developer/Ascend/cann-9.0.0/bin/msprof|

第一张命令报错的原因是将两行粘成了`npu-smi info command -v ...`；第二张已分别执行正确。没有据此观察到设备故障。

仅凭进程列表为空不能保证物理设备独占或未来无并发干扰。管理设备编号与容器内runtime逻辑编号不同；本次运行时只看到一张设备，实际计算使用逻辑编号0。

本地WSL仅对probe_environment.py做了无NPU环境的启动检查，成功输出结构化清单；该本地检查不属于远端上板验证。

## 直接接入与上板证据

复用用户已建立的CANNLab/AtomGit专用SSH配置，远端`pwd`返回`/home/developer`；SCP上传、SSH运行和SCP回收均成功。没有修改SSH设置，也没有把配置、身份文件或私钥加入仓库。

独立远端目录为`/home/developer/ascend-lab-codex-1z1yBI`；未修改cannbot工程。首次非交互运行未加载CANN环境，`npu-smi`报缺少`libc_sec.so`，ACL加载报缺少`libmsprofiler.so`。在该进程内先执行`source /home/developer/Ascend/cann-9.0.0/set_env.sh`后，两项恢复正常；无需重装软件或修改shell启动文件。

|直接验证|结果|
|---|---|
|系统 / Python|Linux aarch64 / 3.11.4|
|CANN工具目录|/home/developer/Ascend/cann-9.0.0|
|bisheng / ccec / msprof|均可运行；msprof op帮助可列出L2Cache、Memory、PipeUtilization等指标|
|构建工具|CMake 3.20.5，g++ 9.4.0|
|ACL初始化 / 查询 / 退出|均返回0；可见设备数1；SoC Ascend910B3|
|已安装且成功导入|torch 2.7.1+cpu，torch_npu 2.7.1.post4|
|真实计算|torch_npu向量Add，16384个float32元素，执行设备npu:0|
|数值验证|与CPU参考逐元素完全一致；最大绝对误差0；全部有限|

证据文件：[stage0.json](接入实测/stage0.json)、[torch_smoke.json](接入实测/torch_smoke.json)。两者均含执行脚本SHA256，可与入库脚本核对。`torch`版本含`+cpu`不代表本环境无法运行NPU；本次扩展执行和结果已证明NPU通路可用。

导入torch_npu时出现库文件所有者与当前用户不一致的警告，未阻止本次运行；没有擅自修改预装库权限。这次检查未编译自定义Ascend C内核，未采集profiling，未比较性能；不能据此报告加速比、带宽或缓存收益。
