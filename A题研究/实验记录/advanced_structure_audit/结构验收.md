# Advanced候选生成结构验收

仅验证正式图的结构覆盖与方案协议，不调用官方evaluate，不验证编译后的内存/Pipe/跨核执行可行性，不产生任何makespan成绩。

- 正式100图×N=1..5，共500槽；本轮完成500槽，结构有效500槽，未覆盖0槽。
- 核验候选共4364份；存在候选族生成失败的槽为0。所有逐槽失败原样记录，不把未覆盖记成通过。
- Python 3.12.14，单进程，295秒计算硬截止；记录与收尾预留5秒。实际总耗时212.948秒。
- 官方evaluate调用尝试：0。
- case_071、N=5候选重复生成一致性：{'case': 'case_071', 'num_cores': 5, 'equal': True, 'comparison': 'entire candidate lists, metadata, plan order and failure lists', 'repeat_elapsed_seconds': 0.04823679197579622}。
- 生成调用路径中按题号写死的字符串常量：[]。独立探针main()中的试验用例清单不在生成路径上。
- 开始/结束全文件哈希变化：[]。
- 生成相关函数AST变化：[]。

本验收不替代原版评估器。结构无环后仍可能因官方补入内存或逐Pipe顺序形成执行问题；耗时不是同预算算法性能实验。
