# 快速局部入口：两例工程实测

固定两例：001/P2/N5、093/P3/N5；从已验证的704份方案快照对应方案出发，每例使用独立空缓存。每例最多9次逻辑调用、120秒软总预算、单worker超时30秒；两例串行，期间P1/P3正式批次仍占用本机资源。

| 配置 | 实际新官方调用 | CLI与搜索观察耗时 | 起点→结果 |
|---|---:|---:|---:|
| 001/P2/N5 | 3 | 0.945秒 | 47502→47502 |
| 093/P3/N5 | 7 | 2.198秒 | 15630→15630 |

共10次成功调用、0缓存命中，总观察耗时3.151秒。所有新成功结果都额外经过完整原始输出核验，初始方案的周期数重现，源/图/配置/计划哈希未变。既有优解均保留，本次没有发现新增性能改善。

这是快速入口的两例小图工程检查，不是12图开发面板、全量成绩或隔离环境速度基准，不能把3.151秒外推到大图/所有配置。软预算提前停止等边界由独立模拟测试覆盖，这两例均正常完成短profile，没有触发时间停止。

可复核文件：`manifest.json`冻结调用前的范围、输入与来源；`summary.json`汇总实际调用和校验结果；各run子目录保存逐候选、官方原始gzip与源码哈希。驱动源码为上级`run_quick_smoke.py`。
