# 克隆、验证与版本管理

仓库：<https://github.com/lrk7353-arch/2026huaweimathmodel_a>。

当前交付为第六轮：最新求解代码、100张原始计算图及原版评测器、六轮导出成果。最新一轮覆盖100张图 × P1/P2/P3 × 1—5核，共1500份方案；相对第五轮25个配置降时，保留方案零回退。论文仍暂缓。

## 六轮成果

| 轮次 | 方案覆盖 | 结果说明 |
|---|---:|---|
| 第一轮（目录名为“本轮成果”） | 1227/1500 | [第一轮结果](A题研究/直接迭代/本轮成果/本轮结果.md) |
| 第二轮 | 1500/1500 | [第二轮结果](A题研究/直接迭代/第二轮成果/第二轮结果.md) |
| 第三轮 | 1500/1500 | [第三轮结果](A题研究/直接迭代/第三轮成果/第三轮结果.md) |
| 第四轮 | 1500/1500 | [第四轮结果](A题研究/直接迭代/第四轮成果/第四轮结果.md) |
| 第五轮 | 1500/1500 | [第五轮结果](A题研究/直接迭代/第五轮成果/第五轮结果.md) |
| 第六轮（当前） | 1500/1500 | [第六轮结果](A题研究/直接迭代/第六轮成果/第六轮结果.md) |

每轮保留自己的方案目录、成绩CSV和汇总JSON，不覆盖上一轮。它们记录各轮累计最好方案，不是同预算方法排行榜。第六轮另附同预算比较、P3机制归因和完整冷启动计时。

## 新电脑开始使用

使用Python 3.12；求解器与当前机制测试只依赖标准库，无需GPU或模型训练。绘图等历史分析脚本可能需要单独安装对应库。

```sh
git clone https://github.com/lrk7353-arch/2026huaweimathmodel_a.git
cd 2026huaweimathmodel_a
python3 -B -m unittest discover -s A题研究/直接迭代 -p 'test_p*.py'
```

直接复评一份第六轮P3五核方案，不依赖原电脑的任何评测缓存：

```sh
python3 -B A题研究/直接迭代/solve.py --case 94 --problem 3 --cores 5 \
  --incumbent-plan A题研究/直接迭代/第六轮成果/方案/p3/n5/case_094_multicore_res.json \
  --budget 1 --seconds 60 --out my_runs/verify_p3_case94
```

预期官方周期为20156。`--budget 1`只复评输入方案；若要继续优化，指定`--p3-refinement joint --budget 13`，其中首次复评占1次，后续搜索最多12次。方案优劣以原版官方评测器为准。

从原始图完整运行P1，不复用历史方案或历史评测缓存：

```sh
python3 -B A题研究/直接迭代/solve.py --case 2 --problem 1 --from-scratch \
  --p1-method portfolio_diverse --budget 12 --seconds 180 \
  --evaluation-timeout 60 --fresh-evaluations --out my_runs/p1_case2_fresh
```

输出目录内`best.plan.json`为最终方案，`summary.json`为运行记录。每次运行使用新的输出目录，避免覆盖。P2/P3从原图开始的完整入口见[统一求解器说明](A题研究/精修求解器/SOLVE.md)；其余模式见[最新版运行说明](A题研究/直接迭代/README.md)。

默认热启动会查找本机历史结果；新克隆应使用上面的`--incumbent-plan`或`--from-scratch`。六轮CSV/JSON中的部分绝对路径用于指向原电脑的官方原始记录，不是新电脑所需输入。历史原始评测缓存和批次运行目录未入库，因此依赖它们的旧批次汇总、进度及报告重生成脚本不能仅凭克隆直接重建历史运行。已导出的报告、成绩和方案可直接查看、复评。

## Git协作约定

- `main`保存可运行的已核对成果；本次交付标记为`round-6`，它同时包含六轮结果，不代表重建了第二至第五轮各自的历史源码。
- 新实验在分支中开发，例如`git switch -c experiment/p3-budget-routing`。提交前查看`git diff`、运行相关检查，再提交代码和结果说明。
- 新轮次成果使用新目录；旧轮结果作为历史依据保留。原始题目、数据、配置与官方评测器保持原样。
- `运行结果/`、历史`runs/`、`evaluations/`、环境目录、`my_runs/`及旧交接包被忽略。需要分享新成果时，明确加入该轮精选方案和汇总，不用`git add -f`把整套缓存塞进仓库。
- 推送采用正常提交及合并流程，避免重写共享`main`历史或覆盖已有轮次标签。拉取前先处理本地改动，使用`git pull --ff-only`检查是否可直接同步。

六轮大量方案文件相同，Git会复用相同文件对象。检出目录仍需为数据及六轮方案留出约1GB空间，克隆传输体积通常明显更小。
